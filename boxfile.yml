run.config:
  engine: python
  engine.config:
    runtime: python-3.6
  extra_packages:
    - nodejs
  cache_dirs:
    - node_modules
  extra_steps:
    - npm install
  fs_watch: true
web.client:
  start: npm run serve
data.db:
  image: nanobox/postgresql:9.5